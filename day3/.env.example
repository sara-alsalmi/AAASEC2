# OpenRouter key (sk-or-...) — see day1/README.md OpenRouter section
OPENAI_API_KEY=sk-or-REPLACE_ME

# Your identity on the class network (used in your Agent Card)
STUDENT_NAME=your-github-username
# Where OTHER students can reach your agent (set on the shared server; localhost while local)
PUBLIC_URL=http://localhost:8000

# Run without any API keys (deterministic fake agent — plumbing still works)
# USE_FAKE=1
